<!-- Auto-generated by wt dev gen-spec — DO NOT EDIT -->

# Work Tools CLI Reference

This document is automatically generated from the `--help` output of the `wt` command.

# `wt`

```
Usage: wt [OPTIONS] COMMAND [ARGS]...

  Work Tools CLI – Unified entry point for all automation tools.

Options:
  --help  Show this message and exit.

Commands:
  browser   Browser session utilities — extract auth tokens/cookies from...
  dev       Development and maintenance utilities.
  docs      Docs Context Reader / Agent Skill
  git       Git Repository Log Viewer
  ims       IMS Document Client
  init      Initialize the environment (.env file) interactively.
  taiga     Taiga API Client / Agent Skill
  workflow  Workflow CLI – cross-module automation commands
```

## `wt browser`

```
Usage: wt browser [OPTIONS] COMMAND [ARGS]...

  Browser session utilities — extract auth tokens/cookies from Chrome.

Options:
  --help  Show this message and exit.

Commands:
  chrome-start  Start Chrome with --remote-debugging-port for CDP access.
  session       Extract session info (localStorage / cookies) from a...
```

### `wt browser chrome-start`

```
Usage: wt browser chrome-start [OPTIONS]

  Start Chrome with --remote-debugging-port for CDP access.

  This is required on Windows/Linux before using session extraction commands.
  On macOS this is optional (AppleScript is used by default).

Options:
  -p, --port INTEGER  Remote debugging port.  [default: 9222]
  --chrome-path TEXT  Explicit path to the Chrome executable. Auto-detected if
                      omitted.
  --profile TEXT      Chrome profile directory name (e.g. 'Default', 'Profile
                      1').
  --help              Show this message and exit.
```

### `wt browser session`

```
Usage: wt browser session [OPTIONS] DOMAIN

  Extract session info (localStorage / cookies) from a Chrome tab.

  DOMAIN is the domain substring to match against open Chrome tab URLs.

  Examples:
    wt browser session example.com -ls authToken
    wt browser session myapp.io -ck session_id -cp wordpress_logged_in
    wt browser session example.com -ls token --backend cdp --port 9222

Options:
  -ls, --local-storage TEXT       localStorage key(s) to retrieve. Repeatable.
  -ck, --cookie TEXT              Cookie name(s) to retrieve (exact match).
                                  Repeatable.
  -cp, --cookie-prefix TEXT       Cookie name prefix(es) to retrieve.
                                  Repeatable.
  --backend [auto|applescript|cdp]
                                  Session extraction backend.  [default: auto]
  -p, --port INTEGER              CDP port (only used with --backend=cdp).
                                  [default: 9222]
  --json                          Output as JSON.
  --help                          Show this message and exit.
```

---

## `wt dev`

```
Usage: wt dev [OPTIONS] COMMAND [ARGS]...

  Development and maintenance utilities.

Options:
  --help  Show this message and exit.

Commands:
  gen-spec  Generate API specification documentation (api_spec.md).
```

### `wt dev gen-spec`

```
Usage: wt dev gen-spec [OPTIONS]

  Generate API specification documentation (api_spec.md).

Options:
  --help  Show this message and exit.
```

---

## `wt docs`

```
Usage: wt docs [OPTIONS] COMMAND [ARGS]...

  Docs Context Reader / Agent Skill

Options:
  --help  Show this message and exit.

Commands:
  read-docs  Read and compose context for a SUBJECT defined in...
```

### `wt docs read-docs`

```
Usage: wt docs read-docs [OPTIONS] SUBJECT

  Read and compose context for a SUBJECT defined in docs_manifest.yaml.

  Available subjects:

  * api-reference: API specification reference for CLI tools.

  * task-writer: AI skill context for automated management of Taiga user
  stories and tasks.

Options:
  --help  Show this message and exit.
```

---

## `wt git`

```
Usage: wt git [OPTIONS] COMMAND [ARGS]...

  Git Repository Log Viewer

Options:
  --help  Show this message and exit.

Commands:
  commit-info  Gather staged diff, recent logs, and commit style guide...
  log          Fetch commit logs with diffs.
```

### `wt git commit-info`

```
Usage: wt git commit-info [OPTIONS] [EXTRA]...

  Gather staged diff, recent logs, and commit style guide for AI-assisted
  commit message generation.

  Any trailing arguments are passed through as extra instructions.

Options:
  -m, --module TEXT  Module name to prefix the commit subject with [module].
  -v, --verbose      Request a detailed body in addition to the subject line.
  --help             Show this message and exit.
```

### `wt git log`

```
Usage: wt git log [OPTIONS] [COMMIT_INPUT]

  Fetch commit logs with diffs.

  COMMIT_INPUT formats: <SHA>, <SHA1>..<SHA2>, <SHA>~<N>

  Use --branch to automatically detect the commit range for the current
  feature branch (must start with 'f/').

Options:
  -b, --branch  Auto-detect feature branch range (f/... → base..HEAD).
  --help        Show this message and exit.
```

---

## `wt ims`

```
Usage: wt ims [OPTIONS] COMMAND [ARGS]...

  IMS Document Client

Options:
  --help  Show this message and exit.

Commands:
  get-document           Fetch and display an IMS document by UID
  get-document-from-url  Fetch and display an IMS document by URL
  get-documents          Fetch and display multiple IMS documents
```

### `wt ims get-document`

```
Usage: wt ims get-document [OPTIONS]

  Fetch and display an IMS document by UID

Options:
  --uid TEXT  Document UID (numeric string)  [required]
  --help      Show this message and exit.
```

### `wt ims get-document-from-url`

```
Usage: wt ims get-document-from-url [OPTIONS]

  Fetch and display an IMS document by URL

Options:
  --url TEXT  Document URL  [required]
  --help      Show this message and exit.
```

### `wt ims get-documents`

```
Usage: wt ims get-documents [OPTIONS]

  Fetch and display multiple IMS documents

Options:
  --uid TEXT  Document UIDs (repeatable)  [required]
  --help      Show this message and exit.
```

---

## `wt init`

```
Usage: wt init [OPTIONS]

  Initialize the environment (.env file) interactively.

Options:
  --help  Show this message and exit.
```

---

## `wt taiga`

```
Usage: wt taiga [OPTIONS] COMMAND [ARGS]...

  Taiga API Client / Agent Skill

Options:
  --help  Show this message and exit.

Commands:
  create-task       Create one or multiple tasks for an existing US by #ref
  create-userstory  Create a User Story with tasks and custom attributes...
  update-task       Update an existing Task's fields by #ref
  update-userstory  Update an existing User Story's fields and/or custom...
```

### `wt taiga create-task`

```
Usage: wt taiga create-task [OPTIONS]

  Create one or multiple tasks for an existing US by #ref

Options:
  --us-ref INTEGER    User Story #ref number to link to  [required]
  --subject TEXT      Task subject (single task)
  --description TEXT  Task description (single task)
  --tasks TEXT        Task subjects (repeatable). Use 'subject::description'
                      for bulk mode with descriptions.
  --tasks-json TEXT   JSON array of tasks: '[{"subject": "...", "description":
                      "..."}]'
  --me                Assign task(s) to myself
  --help              Show this message and exit.
```

### `wt taiga create-userstory`

```
Usage: wt taiga create-userstory [OPTIONS]

  Create a User Story with tasks and custom attributes in one go

Options:
  --subject TEXT         User Story subject  [required]
  --description TEXT     User Story description
  --status INTEGER       Initial status ID
  --tasks TEXT           Task subjects (repeatable). Use
                         'subject::description' for bulk mode with
                         descriptions.
  --tasks-json TEXT      JSON array of tasks: '[{"subject": "...",
                         "description": "..."}]'
  --custom-attrs TEXT    Custom attributes as 'attr_id::value' (repeatable)
  --me                   Assign everything to myself
  --assigned-to INTEGER  Assign to specific user ID
  --help                 Show this message and exit.
```

### `wt taiga update-task`

```
Usage: wt taiga update-task [OPTIONS]

  Update an existing Task's fields by #ref

Options:
  --ref INTEGER          Task #ref number  [required]
  --subject TEXT         New subject
  --description TEXT     New description
  --status INTEGER       New status ID
  --me                   Assign to myself
  --assigned-to INTEGER  Assign to specific user ID
  --help                 Show this message and exit.
```

### `wt taiga update-userstory`

```
Usage: wt taiga update-userstory [OPTIONS]

  Update an existing User Story's fields and/or custom attributes

Options:
  --ref INTEGER          User Story #ref number  [required]
  --subject TEXT         New subject
  --description TEXT     New description
  --status INTEGER       New status ID
  --custom-attrs TEXT    Custom attributes to update as 'attr_id::value'
                         (repeatable)
  --me                   Assign to myself
  --assigned-to INTEGER  Assign to user ID
  --help                 Show this message and exit.
```

---

## `wt workflow`

```
Usage: wt workflow [OPTIONS] COMMAND [ARGS]...

  Workflow CLI – cross-module automation commands

Options:
  --help  Show this message and exit.

Commands:
  get-context  Get full context of a User Story (US, tasks, comments, IMS...
```

### `wt workflow get-context`

```
Usage: wt workflow get-context [OPTIONS]

  Get full context of a User Story (US, tasks, comments, IMS docs)

Options:
  --ref TEXT  User Story #ref number  [required]
  --help      Show this message and exit.
```

---
